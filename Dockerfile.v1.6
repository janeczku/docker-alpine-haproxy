FROM alpine:3.4

ARG HAPROXY_VERSION
ENV HAPROXY_VERSION ${HAPROXY_VERSION}
ARG WITH_LUA
ENV WITH_LUA ${WITH_LUA}

COPY build.sh /build.sh
RUN /build.sh

CMD [ "/usr/sbin/haproxy", "-f", "/etc/haproxy/haproxy.cfg", "-db" ]
